{
  "name": "bruce-clock",
  "version": "1.3.0",
  "description": "Bruce Clock",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "author": "",
  "license": "ISC",
  "build": {
    "appId": "bruceStudio.public.bruceClock",
    "win": {
      "target": "portable",
      "icon": "src/assets/app-icon.png",
      "extraResources": [
        {
          "from": "src/assets/",
          "to": "assets/"
        }
      ]
    }
  },
  "devDependencies": {
    "electron": "^9.4.0",
    "electron-builder": "^22.1.0"
  }
}
